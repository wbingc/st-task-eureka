server:
  port: 8082
  
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost
    
logging:
  level:
    '[com.netflix.eureka]': off
    '[com.netflix.discovery]': off
